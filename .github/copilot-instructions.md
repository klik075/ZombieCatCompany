# Copilot Instructions

## General Guidelines
- First general instruction
- Second general instruction

## Game Development Instructions
- Ensure that the `NewGame()` function can be called from the Lobby (without a tilemap) and handle the case where the boss cannot be summoned at runtime by saving the boss information to `GameData.MemberSaveData`.
